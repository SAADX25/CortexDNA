<Window x:Class="CortexDNA.CleanConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Scan Complete" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="#00ADEF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33BBFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0088CC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#252526" BorderBrush="#444" BorderThickness="1" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Icon -->
                <RowDefinition Height="Auto"/> <!-- Title -->
                <RowDefinition Height="Auto"/> <!-- Stats -->
                <RowDefinition Height="*"/>    <!-- List -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- 1. Icon -->
            <Viewbox Height="64" Width="64" HorizontalAlignment="Center" Margin="0,0,0,15">
                <Path Data="M19.89,10.74C19.36,11.5 19.89,12.56 20.81,12.56H25.37C26.93,12.56 28.18,13.82 28.18,15.37V29.44C28.18,31 26.93,32.25 25.37,32.25H6.62C5.06,32.25 3.81,31 3.81,29.44V15.37C3.81,13.82 5.06,12.56 6.62,12.56H11.18C12.1,12.56 12.63,11.5 12.1,10.74L10.21,8H21.78L19.89,10.74M21.5,15.37H10.5V19.12H21.5V15.37M21.5,21H10.5V24.75H21.5V21Z" 
                      Fill="#00ADEF" />
            </Viewbox>

            <!-- 2. Title -->
            <TextBlock Grid.Row="1" Text="Scan Complete" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Grid.Row="2" x:Name="TxtSummary" Text="Found 0 files taking up 0 MB" FontSize="16" Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- 3. List -->
            <Border Grid.Row="3" Background="#1E1E1E" CornerRadius="8" Padding="15" Margin="0,0,0,25">
                <StackPanel>
                    <TextBlock Text="Locations to be cleaned:" Foreground="#888" FontSize="12" Margin="0,0,0,10"/>
                    <StackPanel x:Name="ListLocations">
                        <!-- Items will be bullet points -->
                        <TextBlock Text="• User Temporary Files" Foreground="#AAA" Margin="0,0,0,5"/>
                        <TextBlock Text="• System Temp Folder" Foreground="#AAA" Margin="0,0,0,5"/>
                        <TextBlock Text="• Windows Prefetch" Foreground="#AAA" Margin="0,0,0,5"/>
                        <TextBlock Text="• Windows Update Cache" Foreground="#AAA" Margin="0,0,0,5"/>
                        <TextBlock Text="• Error Reporting Logs" Foreground="#AAA" Margin="0,0,0,5"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 4. Buttons -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Click="BtnCancel_Click"/>
                <Button Grid.Column="2" Content="Clean Now" Style="{StaticResource PrimaryButtonStyle}" Click="BtnClean_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>